/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class PlaybookCardDetailsModel extends BaseModel {
    static resource = 'playbook_card_details';
    constructor(properties) {
        super(properties);
    }
}
