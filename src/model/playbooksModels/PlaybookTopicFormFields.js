/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class PlaybookTopicFormFields extends BaseModel {
    static resource = 'playbook_topic_form_fields_personas';
    constructor(properties) {
        super(properties);
    }
}
