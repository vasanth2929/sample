/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class PlaybookTopicCard extends BaseModel {
    static resource = 'playbook_topic_card_personas';
    constructor(properties) {
        super(properties);
    }
}
