/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class StoriesCustEnvsModel extends BaseModel {
    static resource = 'stories_cust_envs';

    constructor(properties) {
        super(properties);
    }
}
