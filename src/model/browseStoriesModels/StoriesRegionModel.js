/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class StoriesRegionModel extends BaseModel {
    static resource = 'stories_region';

    constructor(properties) {
        super(properties);
    }
}
