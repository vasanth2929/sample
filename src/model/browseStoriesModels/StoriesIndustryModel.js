/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class StoriesIndustryModel extends BaseModel {
    static resource = 'stories_industry';

    constructor(properties) {
        super(properties);
    }
}
