/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class DealCardMetadataModel extends BaseModel {
    static resource = 'dealCardMetadata';
    constructor(properties) {
        super(properties);
    }
}
