/* eslint-disable no-useless-constructor */
import { BaseModel } from '../BaseModel';

export class NewDealCardMetadataModel extends BaseModel {
    static resource = 'newDealCardMetadata';
    constructor(properties) {
        super(properties);
    }
}
